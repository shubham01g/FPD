node_modules/
dist/
dist-ssr/
*.local
.env
.env.local
supabase/.temp
.DS_Store
FPD.zip
.vercel

# Internal engagement / working notes — not for the client repo
memory/

# Local scratch: screenshot scripts and their output, plus loose source
# assets dropped in the project root. Nothing here is referenced by src/.
shots/
screenshot_*.cjs
family-story-section.png
*.mp4
*.zip
